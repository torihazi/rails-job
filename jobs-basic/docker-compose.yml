services:
  # Railsアプリケーションのみ
  web:
    build: .
    command: bash -c "rm -f tmp/pids/server.pid && bundle exec rails s -b '0.0.0.0'"
    volumes:
      # ローカルのコードをコンテナと同期（編集がすぐ反映される）
      - .:/app
    ports:
      - "3000:3000"
    stdin_open: true
    tty: true